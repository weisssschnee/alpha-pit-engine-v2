# Deprecated Graph Compatibility Namespace

This directory cannot assert project status. `graph.json` and `graph.html` are
compatibility pointers only. Current status is owned exclusively by
`../architecture/architecture_registry.json`; observed source snapshots live
under `../codegraph/` and are bound to a full repository SHA.
